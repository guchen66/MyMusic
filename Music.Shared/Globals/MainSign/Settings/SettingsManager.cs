
namespace Music.Shared.Globals.MainSign.Settings
{
    public static class SettingsManager
    {
        /*private const string FilePath = "settings.json";

        public static void SaveSettings(SetDetailData data)
        {
            string json = JsonConvert.SerializeObject(data);
            File.WriteAllText(FilePath, json);
        }

        public static SetDetailData LoadSettings()
        {
            if (File.Exists(FilePath))
            {
                string json = File.ReadAllText(FilePath);
                return JsonConvert.DeserializeObject<SetDetailData>(json);
            }
            else
            {
                return new SetDetailData();
            }
        }*/
    }
}
